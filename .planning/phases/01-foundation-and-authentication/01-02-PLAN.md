---
phase: 01-foundation-and-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/shared/auth/oidc-config.ts
  - src/shared/auth/useAuthRoles.ts
  - src/shared/api/client.ts
  - src/shared/ui/AppLayout.tsx
  - src/shared/ui/NavBar.tsx
  - src/shared/ui/Sidebar.tsx
  - src/shared/ui/MobileNav.tsx
  - src/routes/__root.tsx
  - src/routes/index.tsx
  - src/routes/_authenticated.tsx
  - src/routes/_authenticated/dashboard.tsx
  - src/main.tsx
  - src/shared/auth/useAuthRoles.test.ts
  - src/shared/auth/oidc-config.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - FOUND-05

must_haves:
  truths:
    - "OIDC config points to Keycloak with PKCE, automaticSilentRenew enabled, onSigninCallback cleans URL"
    - "useAuthRoles extracts roles from JWT realm_access claims and exposes isAdmin boolean"
    - "API client attaches Bearer token from session storage to all requests"
    - "NavBar shows app name, nav links, login/logout button, and admin link only for ADMIN role"
    - "AppLayout renders top nav on desktop, bottom nav on mobile, with collapsible sidebar"
    - "Protected routes redirect unauthenticated users via Keycloak signinRedirect"
    - "Layout is responsive: mobile-first with tablet (768px) and desktop (1024px) breakpoints"
  artifacts:
    - path: "src/shared/auth/oidc-config.ts"
      provides: "Keycloak OIDC configuration"
      exports: ["getOidcConfig"]
    - path: "src/shared/auth/useAuthRoles.ts"
      provides: "Role extraction from JWT claims"
      exports: ["useAuthRoles"]
    - path: "src/shared/api/client.ts"
      provides: "Authenticated HTTP client"
      exports: ["apiClient"]
    - path: "src/shared/ui/AppLayout.tsx"
      provides: "App shell layout with nav, sidebar, content area"
      exports: ["AppLayout"]
    - path: "src/shared/ui/NavBar.tsx"
      provides: "Top navigation bar"
      exports: ["NavBar"]
    - path: "src/routes/__root.tsx"
      provides: "Root route with layout and auth context"
      exports: ["Route"]
    - path: "src/routes/_authenticated.tsx"
      provides: "Auth guard layout route"
      exports: ["Route"]
    - path: "src/main.tsx"
      provides: "App entry with AuthProvider + QueryClientProvider + RouterProvider"
      exports: ["default"]
  key_links:
    - from: "src/main.tsx"
      to: "src/shared/auth/oidc-config.ts"
      via: "AuthProvider receives oidcConfig"
      pattern: "AuthProvider.*oidcConfig"
    - from: "src/routes/__root.tsx"
      to: "src/shared/ui/AppLayout.tsx"
      via: "Root route component renders AppLayout"
      pattern: "AppLayout"
    - from: "src/routes/_authenticated.tsx"
      to: "react-oidc-context"
      via: "beforeLoad checks auth.isAuthenticated"
      pattern: "isAuthenticated"
    - from: "src/shared/ui/NavBar.tsx"
      to: "src/shared/auth/useAuthRoles.ts"
      via: "Conditional admin nav rendering"
      pattern: "useAuthRoles|isAdmin"
    - from: "src/shared/api/client.ts"
      to: "ky"
      via: "beforeRequest hook injects Bearer token"
      pattern: "Authorization.*Bearer"
---

<objective>
Wire up Keycloak PKCE authentication via react-oidc-context, create the responsive app shell layout (NavBar, Sidebar, MobileNav), establish TanStack Router file-based routing with auth guards, and configure the API client with automatic Bearer token injection.

Purpose: This plan delivers the core auth flow and app shell that all feature pages will render inside. Users can log in, stay logged in across refreshes, log out, and see role-appropriate navigation.
Output: A working authenticated SPA shell with responsive layout and protected routes.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-and-authentication/01-VALIDATION.md
@.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- components and utilities the executor will import -->

From src/shared/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/shared/config/env.ts:
```typescript
interface AppConfig {
  apiBaseUrl: string
  keycloakUrl: string
  keycloakRealm: string
  keycloakClientId: string
}
export function getConfig(): AppConfig
```

From src/shared/ui/GlassPanel.tsx:
```typescript
interface GlassPanelProps {
  children: React.ReactNode
  intensity?: 'light' | 'medium' | 'heavy'
  className?: string
  as?: React.ElementType
}
export function GlassPanel(props: GlassPanelProps): JSX.Element
```

From src/shared/ui/ErrorBoundary.tsx:
```typescript
export class ErrorBoundary extends React.Component<{children: React.ReactNode}>
```

From src/test/mocks/auth.ts:
```typescript
export function createMockAuth(overrides?: Partial<AuthContextProps>): AuthContextProps
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement auth utilities, OIDC config, API client, and role hook</name>
  <files>
    src/shared/auth/oidc-config.ts,
    src/shared/auth/useAuthRoles.ts,
    src/shared/api/client.ts,
    src/shared/auth/oidc-config.test.ts,
    src/shared/auth/useAuthRoles.test.ts
  </files>
  <behavior>
    oidc-config:
    - getOidcConfig() returns AuthProviderProps with authority built from keycloakUrl + realm
    - client_id from config
    - redirect_uri and post_logout_redirect_uri set to window.location.origin
    - scope: 'openid profile'
    - automaticSilentRenew: true
    - onSigninCallback clears URL query params via window.history.replaceState

    useAuthRoles:
    - Returns { roles, isAdmin, isUser, hasRole } from JWT realm_access.roles
    - When user has ADMIN role, isAdmin is true
    - When user has no roles, returns empty array and false for isAdmin/isUser
    - hasRole('ADMIN') returns true when ADMIN is in roles

    apiClient:
    - ky instance with prefixUrl from getConfig().apiBaseUrl
    - beforeRequest hook reads user from sessionStorage and sets Authorization header
  </behavior>
  <action>
    1. Write tests first:
       - `src/shared/auth/oidc-config.test.ts`: Test that getOidcConfig returns correct authority URL, automaticSilentRenew is true, scope is 'openid profile'. Mock getConfig to return test values.
       - `src/shared/auth/useAuthRoles.test.ts`: Test with admin user (realm_access.roles includes 'ADMIN'), regular user (includes 'USER' only), and unauthenticated user (null). Use renderHook with a wrapper that provides mock auth context.

    2. Create `src/shared/auth/oidc-config.ts` per RESEARCH.md Pattern 2. Uses getConfig() for Keycloak URL/realm/clientId. Returns AuthProviderProps object.

    3. Create `src/shared/auth/useAuthRoles.ts` per RESEARCH.md Pattern 6. Extracts realm_access.roles from auth.user.profile, returns { roles, isAdmin, isUser, hasRole }.

    4. Create `src/shared/api/client.ts` per RESEARCH.md Pattern 3. ky.create with prefixUrl from getConfig().apiBaseUrl, beforeRequest hook reads user from sessionStorage using the oidc storage key pattern `oidc.user:{authority}:{clientId}`.

    5. Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/cookbook-web-frontend && npx vitest run src/shared/auth/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    getOidcConfig returns correct Keycloak PKCE configuration with automaticSilentRenew. useAuthRoles correctly extracts admin/user roles from JWT claims. API client injects Bearer token. All auth tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create responsive app shell layout, routes, and wire providers in main.tsx</name>
  <files>
    src/shared/ui/AppLayout.tsx,
    src/shared/ui/NavBar.tsx,
    src/shared/ui/Sidebar.tsx,
    src/shared/ui/MobileNav.tsx,
    src/routes/__root.tsx,
    src/routes/index.tsx,
    src/routes/_authenticated.tsx,
    src/routes/_authenticated/dashboard.tsx,
    src/main.tsx
  </files>
  <action>
    1. Create `src/shared/ui/NavBar.tsx`:
       - Fixed top bar with GlassPanel (intensity: 'heavy') as background
       - Left: App logo/name "Cookbook" (text-accent color)
       - Center: Nav links -- Home, Recipes (placeholder), Meal Plans (placeholder). Use TanStack Router Link components
       - Right: User menu area. When authenticated (useAuth): show user display name + logout button (calls auth.signoutRedirect()). When not authenticated: show "Login" button (calls auth.signinRedirect())
       - Admin link: Only visible when useAuthRoles().isAdmin is true. Links to "/admin" (placeholder route for Phase 5)
       - Responsive: hide center nav links on mobile (shown in MobileNav instead)
       - Use Lucide icons: Menu, LogOut, User, ChefHat (logo)

    2. Create `src/shared/ui/Sidebar.tsx`:
       - Collapsible left sidebar (hidden on mobile, toggleable on tablet/desktop)
       - GlassPanel container
       - Placeholder content: "Tags" heading (will be populated in Phase 2 for tag browsing)
       - Zustand store for sidebar open/closed state (create inline or in a small store file)
       - Width: 256px open, 0 closed, with transition

    3. Create `src/shared/ui/MobileNav.tsx`:
       - Fixed bottom navigation bar, visible only below 768px (md: breakpoint hides it)
       - GlassPanel background
       - Icons for: Home, Recipes, Meal Plans, Profile
       - Use TanStack Router Link with active state highlighting (accent color)

    4. Create `src/shared/ui/AppLayout.tsx`:
       - Composes NavBar (top), Sidebar (left), main content area (center), MobileNav (bottom on mobile)
       - Main content area: black background, min-h-screen, padding for nav offsets
       - Content area wraps children in a div with route transition classes (route-enter/route-enter-active) using CSS transitions
       - Mobile: no sidebar, bottom nav, top nav simplified
       - Tablet/Desktop: top nav, optional sidebar, no bottom nav

    5. Create TanStack Router route files:
       - `src/routes/__root.tsx`: createRootRouteWithContext<{ auth: AuthContextProps }>. Component renders AppLayout wrapping Outlet.
       - `src/routes/index.tsx`: Home page placeholder. Render a GlassPanel with "Welcome to Cookbook" heading and brief text. This is the public landing page.
       - `src/routes/_authenticated.tsx`: Layout route with beforeLoad guard. If !context.auth.isAuthenticated, call context.auth.signinRedirect() and throw redirect to '/'. Component renders Outlet.
       - `src/routes/_authenticated/dashboard.tsx`: Protected placeholder page. Render "Dashboard" heading in a GlassPanel. Demonstrates the auth guard works.

    6. Update `src/main.tsx` to wire all providers per RESEARCH.md Pattern 2:
       - Import and configure AuthProvider with getOidcConfig()
       - Create QueryClient and wrap with QueryClientProvider
       - Create router from routeTree with context: { auth: undefined! }
       - Register router type via module declaration
       - InnerApp component: useAuth() -> pass to RouterProvider context
       - Render tree: StrictMode > AuthProvider > QueryClientProvider > InnerApp
       - Import TanStack Query DevTools (lazy, dev only)

    7. Delete any Vite template files that are no longer needed (App.tsx, App.css, assets/ if present).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Lukas/IdeaProjects/cookbook-web-frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    App builds successfully. NavBar shows app name and login/logout. Sidebar is collapsible. MobileNav renders at mobile widths. Routes are file-based with TanStack Router. Protected /dashboard route has auth guard. Main.tsx wires AuthProvider + QueryClientProvider + RouterProvider. Layout is responsive with mobile-first approach.
  </done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds
- `npx vitest run --reporter=verbose` all tests pass (including Plan 01 tests)
- Auth configuration has automaticSilentRenew: true
- NavBar has logout button using signoutRedirect
- _authenticated route has beforeLoad guard checking isAuthenticated
- Admin nav item conditionally rendered based on ADMIN role
</verification>

<success_criteria>
- OIDC config correctly targets Keycloak with PKCE and silent renew
- Role hook extracts ADMIN/USER from JWT claims
- API client injects Bearer token on all requests
- App shell has top nav, collapsible sidebar, mobile bottom nav
- Protected routes redirect unauthenticated users to Keycloak login
- Layout adapts responsively across mobile/tablet/desktop breakpoints
- Application builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-02-SUMMARY.md`
</output>
