---
phase: 02-recipe-browsing-ratings-and-profiles
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/routes/_authenticated/recipes.tsx
  - src/features/recipes/ui/RecipeCard.tsx
  - src/features/recipes/ui/RecipeCardSkeleton.tsx
  - src/features/recipes/ui/RecipeGrid.tsx
  - src/features/recipes/ui/SearchBar.tsx
  - src/features/recipes/ui/TagTree.tsx
  - src/features/recipes/ui/TagChips.tsx
  - src/shared/ui/Sidebar.tsx
  - src/shared/ui/NavBar.tsx
  - src/shared/ui/MobileNav.tsx
  - src/features/favorites/ui/FavoriteButton.tsx
  - src/features/recipes/ui/RecipeGrid.test.tsx
  - src/features/recipes/ui/SearchBar.test.tsx
  - src/features/recipes/ui/TagTree.test.tsx
autonomous: true
requirements:
  - BROWSE-01
  - BROWSE-02
  - BROWSE-03
  - BROWSE-04
  - RATE-01
  - BROWSE-07

must_haves:
  truths:
    - "User can see a grid of recipe cards with hero image, title, cook time, and star rating"
    - "User can search by keyword and see results update dynamically after 300ms debounce"
    - "User can expand/collapse hierarchical tag tree in sidebar and select tags as filters"
    - "Selected tags appear as removable chips below search bar"
    - "User can click 'Load more' to append next page of results"
    - "Count indicator shows 'Showing X of Y recipes'"
    - "Search keyword and tag filters combine with AND logic"
    - "Favorite heart icon appears on each card and toggles on click"
  artifacts:
    - path: "src/routes/_authenticated/recipes.tsx"
      provides: "Recipe browse page with URL-synced search/filter state"
      contains: "validateSearch"
    - path: "src/features/recipes/ui/RecipeGrid.tsx"
      provides: "CSS grid of cards with load-more button"
      contains: "auto-fill"
    - path: "src/features/recipes/ui/RecipeCard.tsx"
      provides: "Individual recipe card with GlassCard, image, metadata, favorite"
      contains: "GlassCard"
    - path: "src/features/recipes/ui/SearchBar.tsx"
      provides: "Debounced search input"
      contains: "useDebounce"
    - path: "src/features/recipes/ui/TagTree.tsx"
      provides: "Hierarchical expandable tag tree"
      contains: "TagTreeNode"
  key_links:
    - from: "src/routes/_authenticated/recipes.tsx"
      to: "src/features/recipes/api/recipe-queries.ts"
      via: "useInfiniteQuery(recipeQueries.list(filters))"
      pattern: "useInfiniteQuery.*recipeQueries\\.list"
    - from: "src/features/recipes/ui/SearchBar.tsx"
      to: "src/shared/hooks/useDebounce.ts"
      via: "import useDebounce"
      pattern: "useDebounce"
    - from: "src/shared/ui/Sidebar.tsx"
      to: "src/features/recipes/ui/TagTree.tsx"
      via: "renders TagTree in sidebar"
      pattern: "TagTree"
    - from: "src/routes/_authenticated/recipes.tsx"
      to: "TanStack Router search params"
      via: "validateSearch for q and tags"
      pattern: "validateSearch"
---

<objective>
Build the recipe browsing page with card grid, debounced search, hierarchical tag filtering in sidebar, and load-more pagination.

Purpose: This is the primary discovery interface -- the first thing users see when they want to find a recipe. Search + filter + pagination are the core browsing interaction.
Output: /recipes route with fully functional browsing, search, filtering, and pagination.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-CONTEXT.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-RESEARCH.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-01-SUMMARY.md

@src/shared/ui/GlassCard.tsx
@src/shared/ui/GlassPanel.tsx
@src/shared/ui/Sidebar.tsx
@src/shared/ui/NavBar.tsx
@src/shared/ui/MobileNav.tsx
@src/shared/ui/AppLayout.tsx
@src/shared/ui/Skeleton.tsx
@src/shared/hooks/useDebounce.ts
@src/shared/ui/StarRating.tsx
@src/shared/ui/EmptyState.tsx
@src/shared/lib/cn.ts
@src/routes/_authenticated.tsx
@src/features/recipes/api/types.ts
@src/features/recipes/api/recipe-api.ts
@src/features/recipes/api/recipe-queries.ts
@src/features/favorites/api/favorite-api.ts
@src/features/favorites/api/favorite-queries.ts
@src/test/utils.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recipe card, skeleton, grid, search bar, tag tree, tag chips, favorite button components</name>
  <files>
    src/features/recipes/ui/RecipeCard.tsx,
    src/features/recipes/ui/RecipeCardSkeleton.tsx,
    src/features/recipes/ui/RecipeGrid.tsx,
    src/features/recipes/ui/SearchBar.tsx,
    src/features/recipes/ui/TagTree.tsx,
    src/features/recipes/ui/TagChips.tsx,
    src/features/favorites/ui/FavoriteButton.tsx
  </files>
  <action>
    **RecipeCard.tsx:**
    - Receives `recipe: RecipeSummary` prop
    - Wraps in `<Link to="/recipes/$recipeId" params={{ recipeId: recipe.id }}>` from TanStack Router
    - Uses GlassCard with `className="p-0 overflow-hidden"` for edge-to-edge image
    - Hero image in `div.relative.aspect-[4/3]` with `img.h-full.w-full.object-cover`
    - Image fallback: if `recipe.imageUrl` is null, show `bg-gradient-to-br from-accent/20 to-accent/5` with centered ChefHat icon (lucide)
    - Use `onError` handler on `<img>` to swap to fallback on broken image URLs
    - FavoriteButton overlaid at `absolute top-2 right-2` -- pass recipeId and isFavorite
    - Below image: `div.p-4.space-y-2` with:
      - Title: `h3.font-semibold.text-white.truncate`
      - Metadata row: `div.flex.items-center.gap-3.text-sm.text-white/60` -- Clock icon + cookTimeMinutes + " min", StarRating component (size="sm", read-only) + averageRating.toFixed(1)
      - Tag chips: `div.flex.gap-1.5.flex-wrap` -- first 2 tags as `span.text-xs.px-2.py-0.5.rounded-full.bg-white/10.text-white/60`
    - Stop click propagation on FavoriteButton so card link doesn't navigate when toggling favorite

    **RecipeCardSkeleton.tsx:**
    - Mirrors RecipeCard layout with Skeleton components
    - aspect-[4/3] rectangular skeleton for image, text skeletons for title/metadata/tags
    - Export and use in RecipeGrid for loading states

    **RecipeGrid.tsx:**
    - Props: `{ recipes: RecipeSummary[]; isLoading: boolean; hasNextPage: boolean; isFetchingNextPage: boolean; fetchNextPage: () => void; totalElements: number }`
    - CSS grid: `grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-4` (locked decision)
    - If isLoading (initial load): render 6 RecipeCardSkeleton
    - If recipes.length === 0 and not loading: render EmptyState with SearchX icon, "No recipes found", "Try different keywords or remove some filters"
    - Map recipes to RecipeCard components
    - Below grid: count indicator "Showing {recipes.length} of {totalElements} recipes" in `text-sm text-white/40`
    - Load more button: if hasNextPage, render button with `px-6 py-2 rounded-lg bg-accent/20 text-accent-light hover:bg-accent/30 transition-colors disabled:opacity-50`. Text: "Loading..." when isFetchingNextPage, "Load more" otherwise
    - If isFetchingNextPage, also show 3 RecipeCardSkeleton appended to grid

    **SearchBar.tsx:**
    - Props: `{ value: string; onChange: (v: string) => void }`
    - Local state `input` initialized from `value` prop
    - useDebounce(input, 300) -- on debounced change, call onChange
    - Sync external value changes (when `value` prop changes, update `input`)
    - Render in GlassPanel: Search icon (lucide), text input with `bg-transparent text-white placeholder-white/30 outline-none flex-1`, X button to clear (only when input is non-empty)
    - Placeholder text: "Search recipes..."

    **TagTree.tsx:**
    - Props: `{ tags: TagNode[]; selectedIds: number[]; onToggle: (tagId: number) => void; isLoading?: boolean }`
    - If isLoading: render 5 text Skeleton lines
    - Recursive TagTreeNode component for each tag:
      - If tag has children: show ChevronRight/ChevronDown toggle button, click to expand/collapse (local state per node)
      - Tag name as button: click calls onToggle(tag.id)
      - Selected tags highlighted: `text-accent-light font-medium` vs default `text-white/60`
      - Children rendered in `ul.ml-4` when expanded
    - Memoize TagTreeNode with React.memo to prevent re-renders on sibling expand/collapse

    **TagChips.tsx:**
    - Props: `{ tags: { id: number; name: string }[]; onRemove: (tagId: number) => void }`
    - If tags.length === 0, render nothing
    - Render `div.flex.flex-wrap.gap-2`: each tag as a chip `span.flex.items-center.gap-1.text-sm.px-3.py-1.rounded-full.bg-accent/20.text-accent-light` with X icon button calling onRemove

    **FavoriteButton.tsx:**
    - Props: `{ recipeId: string; isFavorite: boolean; className?: string }`
    - Uses `useToggleFavorite()` mutation from favorite-queries
    - Renders Heart icon (lucide): filled + `text-red-400` when favorite, outline + `text-white/60 hover:text-red-400` when not
    - onClick: call `e.stopPropagation(); e.preventDefault(); mutation.mutate(recipeId)`
    - Disable button while mutation is pending
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>All recipe UI components render correctly. TypeScript compilation passes. RecipeCard shows image/title/time/rating/tags/favorite. RecipeGrid uses auto-fill CSS grid with load-more. SearchBar debounces input. TagTree expands/collapses hierarchy. FavoriteButton toggles optimistically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recipe browse route, sidebar tag tree integration, nav link updates, and tests</name>
  <files>
    src/routes/_authenticated/recipes.tsx,
    src/shared/ui/Sidebar.tsx,
    src/shared/ui/NavBar.tsx,
    src/shared/ui/MobileNav.tsx,
    src/features/recipes/ui/RecipeGrid.test.tsx,
    src/features/recipes/ui/SearchBar.test.tsx,
    src/features/recipes/ui/TagTree.test.tsx
  </files>
  <behavior>
    - /recipes route validates search params (q: string, tags: number[])
    - Changing search text updates URL after debounce
    - Selecting a tag adds its ID to URL search params
    - Removing a tag chip removes its ID from URL
    - useInfiniteQuery fetches recipes with current filters
    - Load more appends next page
    - Sidebar renders TagTree instead of placeholder text
    - NavBar and MobileNav link to /recipes instead of disabled placeholders
  </behavior>
  <action>
    **src/routes/_authenticated/recipes.tsx:**
    - Use `createFileRoute('/_authenticated/recipes')` with `validateSearch` per research Pattern 3:
      ```
      validateSearch: (search: Record<string, unknown>): RecipeSearch => ({
        q: typeof search.q === 'string' ? search.q : undefined,
        tags: Array.isArray(search.tags)
          ? search.tags.filter((t): t is number => typeof t === 'number')
          : undefined,
      })
      ```
    - Component: `RecipeBrowsePage`
    - Use `Route.useSearch()` to get `{ q, tags }`
    - Use `useNavigate({ from: Route.fullPath })` for updating search params
    - `setSearch(query)`: navigate with `search: (prev) => ({ ...prev, q: query || undefined })`
    - `toggleTag(tagId)`: navigate with functional updater that adds/removes tagId from tags array
    - `removeTag(tagId)`: same as toggleTag (removes)
    - Call `useInfiniteQuery(recipeQueries.list({ q, tagIds: tags }))` -- note: filter params in query key ensures refetch on filter change
    - Flatten pages: `data?.pages.flatMap(p => p.content) ?? []`
    - Get totalElements from `data?.pages[0]?.totalElements ?? 0`
    - Fetch tags with `useQuery(recipeQueries.tags())`, convert to tree with `buildTagTree()`
    - For mobile tag access: render a "Filter" button (`md:hidden`) below search bar that opens a modal/overlay with TagTree. Use local state `showMobileTags` for visibility. Render as a fixed overlay with GlassPanel background.
    - Layout: SearchBar at top (full width), TagChips below search bar if any selected, RecipeGrid below
    - Resolve tag names for TagChips from the fetched tags data (map tagId to tag name)

    **src/shared/ui/Sidebar.tsx:**
    - Accept optional `children` prop (ReactNode)
    - If children provided, render children instead of the placeholder paragraph
    - Keep the "Tags" header with Tags icon
    - The recipes route will render `<Sidebar>` is NOT the pattern -- instead, the Sidebar already renders globally in AppLayout. So instead: make Sidebar accept a `content` prop or use a Zustand slot pattern.
    - BETTER APPROACH: Add a `sidebarContent` atom to the sidebar store: `sidebarContent: ReactNode | null`, `setSidebarContent: (content: ReactNode | null) => void`. Recipes page sets sidebar content on mount, clears on unmount via useEffect. Sidebar renders `sidebarContent` if set, otherwise placeholder.
    - Add to SidebarState: `content: ReactNode | null` and `setContent: (content: ReactNode | null) => void`
    - In Sidebar component: render `content` if non-null, otherwise placeholder text

    **src/shared/ui/NavBar.tsx:**
    - Change the disabled "Recipes" span to a real `<Link to="/recipes">` with active styling
    - Keep "Meal Plans" as disabled span (Phase 4)
    - Add "Favorites" link: `<Link to="/favorites">` with Heart icon (only shown when authenticated -- it's inside the center nav which is already auth-context-aware)
    - Add "Profile" link: `<Link to="/profile">` with User icon

    **src/shared/ui/MobileNav.tsx:**
    - Enable "Recipes" nav item: change `disabled: true` to `disabled: false` and set `to: '/recipes'`
    - Enable "Profile" nav item: set `to: '/profile'`
    - Add Favorites item: `{ to: '/favorites', icon: Heart, label: 'Favorites' }` -- insert between Recipes and Meals
    - Keep "Meals" disabled (Phase 4)

    **Tests (RecipeGrid.test.tsx):**
    - Test: renders recipe cards with title, cook time, rating
    - Test: shows skeleton loading state when isLoading=true
    - Test: shows empty state when no recipes
    - Test: shows "Load more" button when hasNextPage=true
    - Test: shows count indicator "Showing X of Y recipes"
    - Use renderWithProviders from test/utils.tsx, mock data for RecipeSummary

    **Tests (SearchBar.test.tsx):**
    - Test: renders input with placeholder "Search recipes..."
    - Test: calls onChange after debounce delay
    - Test: shows clear button when input is non-empty
    - Use vi.useFakeTimers for debounce testing

    **Tests (TagTree.test.tsx):**
    - Test: renders tag names
    - Test: expands/collapses parent tags on chevron click
    - Test: calls onToggle when tag name clicked
    - Test: highlights selected tags
  </action>
  <verify>
    <automated>npx vitest run src/features/recipes/ui/ && npx tsc -b --noEmit</automated>
  </verify>
  <done>/recipes route loads, displays recipe card grid with search and tag filtering. URL syncs with search params. Sidebar shows tag tree. NavBar and MobileNav have working Recipes/Favorites/Profile links. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc -b --noEmit` passes
- `npx vitest run src/features/recipes/ui/` passes (RecipeGrid, SearchBar, TagTree tests)
- `npm test` passes (all tests green)
- Recipe browse page renders at /recipes with grid layout
- Search debounces and updates URL
- Tag tree renders in sidebar, selections appear as chips
- Load-more button appends results
</verification>

<success_criteria>
- Recipe card grid displays with responsive auto-fill layout (1-4 columns)
- Cards show hero image (with fallback), title, cook time, star rating, 1-2 tag chips, favorite heart
- Search bar debounces at 300ms and updates URL search params
- Tag tree in sidebar is hierarchical with expand/collapse
- Selected tags appear as removable chips below search bar
- Load-more button appends next page, count indicator shows progress
- Search + tags filter with AND logic
- NavBar and MobileNav link to /recipes, /favorites, /profile
</success_criteria>

<output>
After completion, create `.planning/phases/02-recipe-browsing-ratings-and-profiles/02-02-SUMMARY.md`
</output>
