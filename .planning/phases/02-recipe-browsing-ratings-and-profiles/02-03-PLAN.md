---
phase: 02-recipe-browsing-ratings-and-profiles
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/routes/_authenticated/recipes.$recipeId.tsx
  - src/routes/_authenticated/favorites.tsx
  - src/features/recipes/ui/ServingScaler.tsx
  - src/features/recipes/ui/IngredientList.tsx
  - src/features/recipes/ui/StepList.tsx
  - src/features/ratings/ui/RatingStars.tsx
  - src/features/ratings/ui/RatingBreakdown.tsx
  - src/features/ratings/ui/RatingForm.tsx
  - src/features/recipes/ui/ServingScaler.test.tsx
  - src/features/ratings/ui/RatingForm.test.tsx
autonomous: true
requirements:
  - BROWSE-05
  - BROWSE-06
  - BROWSE-07
  - BROWSE-08
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-04

must_haves:
  truths:
    - "User can view recipe detail page with hero image, ingredients, steps, metadata, and rating summary"
    - "User can adjust serving count and ingredient amounts recalculate in real-time"
    - "User can see multi-criteria rating breakdown with per-criterion averages"
    - "User can submit a rating with a score for each active criterion"
    - "User can edit or delete their own existing rating"
    - "User can toggle favorite from the detail page"
    - "User can view their favorites list at /favorites"
  artifacts:
    - path: "src/routes/_authenticated/recipes.$recipeId.tsx"
      provides: "Recipe detail page with all sections"
      contains: "recipeQueries.detail"
    - path: "src/features/recipes/ui/ServingScaler.tsx"
      provides: "Serving count adjuster with +/- buttons"
      exports: ["ServingScaler"]
    - path: "src/features/recipes/ui/IngredientList.tsx"
      provides: "Scaled ingredient list"
      exports: ["IngredientList"]
    - path: "src/features/ratings/ui/RatingForm.tsx"
      provides: "Per-criterion star input form"
      exports: ["RatingForm"]
    - path: "src/routes/_authenticated/favorites.tsx"
      provides: "Favorites list page"
      contains: "favoriteQueries.list"
  key_links:
    - from: "src/routes/_authenticated/recipes.$recipeId.tsx"
      to: "src/features/recipes/api/recipe-queries.ts"
      via: "useQuery(recipeQueries.detail(recipeId))"
      pattern: "recipeQueries\\.detail"
    - from: "src/features/recipes/ui/IngredientList.tsx"
      to: "ServingScaler"
      via: "scaleAmount function using serving ratio"
      pattern: "scaleAmount"
    - from: "src/features/ratings/ui/RatingForm.tsx"
      to: "src/features/ratings/api/rating-api.ts"
      via: "useMutation for submit/update/delete"
      pattern: "submitRating|updateRating|deleteRating"
---

<objective>
Build the recipe detail page with serving scaling, full rating system (display + submit/edit/delete), and the favorites list page.

Purpose: The detail page is where users decide whether to cook a recipe -- seeing ingredients, steps, and community ratings. Rating interaction and favorites are the primary engagement features.
Output: /recipes/$recipeId detail page, /favorites page, serving scaler, rating components.
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-CONTEXT.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-RESEARCH.md
@.planning/phases/02-recipe-browsing-ratings-and-profiles/02-01-SUMMARY.md

@src/shared/ui/GlassPanel.tsx
@src/shared/ui/GlassCard.tsx
@src/shared/ui/Skeleton.tsx
@src/shared/ui/StarRating.tsx
@src/shared/ui/EmptyState.tsx
@src/shared/lib/cn.ts
@src/features/recipes/api/types.ts
@src/features/recipes/api/recipe-api.ts
@src/features/recipes/api/recipe-queries.ts
@src/features/ratings/api/types.ts
@src/features/ratings/api/rating-api.ts
@src/features/ratings/api/rating-queries.ts
@src/features/favorites/api/favorite-api.ts
@src/features/favorites/api/favorite-queries.ts
@src/features/favorites/ui/FavoriteButton.tsx
@src/test/utils.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recipe detail page with serving scaler, ingredient list, step list, and favorite toggle</name>
  <files>
    src/routes/_authenticated/recipes.$recipeId.tsx,
    src/features/recipes/ui/ServingScaler.tsx,
    src/features/recipes/ui/IngredientList.tsx,
    src/features/recipes/ui/StepList.tsx,
    src/features/recipes/ui/ServingScaler.test.tsx
  </files>
  <behavior>
    - ServingScaler: scaleAmount(2, 4, 8) returns 4 (doubles when doubling servings)
    - ServingScaler: scaleAmount(1, 4, 2) returns 0.5 (halves correctly)
    - ServingScaler: scaleAmount(0.33, 1, 3) returns 0.99, rounded to 2 decimals
    - ServingScaler: +/- buttons increment/decrement servings, minimum 1
    - IngredientList: renders each ingredient with scaled amount, unit, and name
    - Detail page fetches recipe by ID and renders all sections
  </behavior>
  <action>
    **ServingScaler.tsx:**
    - Props: `{ originalServings: number; value: number; onChange: (servings: number) => void }`
    - Renders: "Servings" label, minus button (Minus icon), current value in a `span.text-lg.font-bold.text-white.min-w-[3ch].text-center`, plus button (Plus icon)
    - Minus disabled when value <= 1
    - Styled in GlassPanel: `flex items-center gap-3`
    - Buttons: `rounded-full bg-white/10 p-1.5 hover:bg-white/20 transition-colors disabled:opacity-30`
    - Export `scaleAmount(originalAmount: number, originalServings: number, targetServings: number): number` -- `Math.round((originalAmount / originalServings) * targetServings * 100) / 100`
    - Export `formatAmount(amount: number): string` -- return clean number string (no trailing zeros)

    **IngredientList.tsx:**
    - Props: `{ ingredients: RecipeIngredient[]; originalServings: number; targetServings: number }`
    - Renders each ingredient in a `div.flex.items-baseline.gap-2.py-1.5.border-b.border-white/10`:
      - Amount: `span.font-medium.text-white.min-w-[3ch].text-right` -- formatAmount(scaleAmount(ingredient.amount, originalServings, targetServings))
      - Unit: `span.text-white/60` -- ingredient.unit
      - Name: `span.text-white/80` -- ingredient.ingredientName
    - If ingredients is empty: show "No ingredients listed"

    **StepList.tsx:**
    - Props: `{ steps: RecipeStep[] }`
    - Renders ordered list of steps in `ol.space-y-4`:
      - Each step: `li.flex.gap-4`
        - Step number in `span.flex-shrink-0.w-8.h-8.rounded-full.bg-accent/20.text-accent-light.flex.items-center.justify-center.font-bold`
        - Instruction in `p.text-white/80.leading-relaxed`
    - If steps is empty: show "No steps listed"

    **recipes.$recipeId.tsx:**
    - Use `createFileRoute('/_authenticated/recipes/$recipeId')` -- extracts recipeId from route params
    - Fetch recipe: `useQuery(recipeQueries.detail(recipeId))`
    - Local state: `targetServings` initialized from `recipe.servings`
    - Loading state: render skeleton layout (full-width rectangular skeleton for hero, text skeletons for title/metadata, skeleton lines for ingredients/steps)
    - Layout (Claude's discretion per CONTEXT.md):
      - Full-width hero image at top (aspect-[16/9], object-cover, rounded-lg overflow-hidden). FavoriteButton overlaid top-right. Image fallback same as RecipeCard.
      - Below hero: title (h1.text-3xl.font-bold.text-white), metadata row (prep time, cook time, original servings icons)
      - Two-column layout on desktop (`grid md:grid-cols-[1fr_2fr] gap-6`), stacked on mobile:
        - Left column (GlassPanel): ServingScaler at top, then IngredientList below
        - Right column (GlassPanel): StepList
      - Below columns: Rating section (rendered by RatingBreakdown + RatingForm from Plan 03 Task 2 -- for now just render a placeholder div with id="ratings-section" that Task 2 will fill)
    - Back button at top: `<Link to="/recipes">` with ArrowLeft icon and "Back to recipes"

    **ServingScaler.test.tsx:**
    - Test: scaleAmount(2, 4, 8) === 4
    - Test: scaleAmount(1, 4, 2) === 0.5
    - Test: scaleAmount(0.33, 1, 3) === 0.99
    - Test: scaleAmount(0, 4, 8) === 0 (handles zero)
    - Test: formatAmount(1.00) === "1"
    - Test: formatAmount(0.5) === "0.5"
    - Test: formatAmount(1.25) === "1.25"
    - Test: ServingScaler renders and increments/decrements (render with renderWithProviders, click +/- buttons, verify onChange called)
  </action>
  <verify>
    <automated>npx vitest run src/features/recipes/ui/ServingScaler.test.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <done>Recipe detail page renders at /recipes/$recipeId with hero image, ingredients (scaled by serving count), steps, metadata. ServingScaler tested with arithmetic and UI interaction. Back navigation works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rating display, rating form, favorites list page</name>
  <files>
    src/features/ratings/ui/RatingStars.tsx,
    src/features/ratings/ui/RatingBreakdown.tsx,
    src/features/ratings/ui/RatingForm.tsx,
    src/routes/_authenticated/favorites.tsx,
    src/features/ratings/ui/RatingForm.test.tsx
  </files>
  <behavior>
    - RatingBreakdown displays each criterion name with its average score as StarRating
    - RatingForm shows active criteria with interactive StarRating for each
    - RatingForm submit sends all criterion scores
    - RatingForm in edit mode pre-fills existing scores
    - RatingForm delete button removes the user's rating
    - Favorites page lists user's favorited recipes using RecipeCard grid
  </behavior>
  <action>
    **RatingStars.tsx:**
    - This is a THIN wrapper that re-exports or composes the shared StarRating component with recipe-specific defaults
    - Props: `{ rating: number; size?: 'sm' | 'md' | 'lg'; showValue?: boolean }`
    - Renders StarRating (read-only) + optionally the numeric value in `span.text-sm.text-white/60.ml-1`
    - Used on recipe cards (size="sm") and detail page header (size="md")

    **RatingBreakdown.tsx:**
    - Props: `{ recipeId: string }`
    - Fetches `useQuery(ratingQueries.summary(recipeId))`
    - Loading: skeleton lines
    - Renders inside a GlassPanel:
      - Header: "Ratings" with overall average as large StarRating (size="lg") + `totalRatings` count ("X ratings")
      - If totalRatings === 0: show "No ratings yet. Be the first to rate this recipe."
      - Per-criterion breakdown: vertical list of `div.flex.items-center.justify-between.py-2`:
        - Criterion name in `text-white/80`
        - StarRating (size="sm", read-only) showing criterion average + numeric value

    **RatingForm.tsx:**
    - Props: `{ recipeId: string }`
    - Fetches `useQuery(ratingQueries.criteria())` for active criteria list
    - Fetches `useQuery(ratingQueries.userRating(recipeId))` for existing user rating
    - If user has existing rating: show "Edit your rating" header, pre-fill scores, show delete button
    - If no existing rating: show "Rate this recipe" header
    - For each criterion: render criterion name + interactive StarRating (onChange sets score in local state `Record<number, number>`)
    - Local state: `scores: Record<number, number>` -- criterionId to score (1-5)
    - Submit button: disabled if not all criteria have scores. Text: "Update rating" (edit mode) or "Submit rating" (new mode)
    - On submit: call `submitRating` or `updateRating` mutation, then invalidate rating queries
    - On delete: confirmation prompt (`window.confirm`), then call `deleteRating` mutation, invalidate queries
    - Mutations:
      - `useMutation({ mutationFn: ... , onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['ratings'] }); queryClient.invalidateQueries({ queryKey: ['recipes'] }) } })`
    - Loading criteria: show skeleton
    - Wrap form in GlassPanel

    **Now update recipes.$recipeId.tsx:** Replace the ratings placeholder with actual RatingBreakdown + RatingForm components below the ingredients/steps columns.

    **favorites.tsx:**
    - Use `createFileRoute('/_authenticated/favorites')` with component `FavoritesPage`
    - Fetch `useQuery(favoriteQueries.list())`
    - Loading: render 6 RecipeCardSkeleton in a grid
    - Empty: EmptyState with Heart icon, "No favorites yet", "Heart a recipe to save it here", with a Link to /recipes as action button
    - Render favorites in same CSS grid as recipe browse: `grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-4`
    - Each favorite rendered as RecipeCard (import from recipes feature)
    - Page title: "My Favorites" as h1

    **RatingForm.test.tsx:**
    - Test: renders criterion names from mock criteria data
    - Test: submit button disabled when not all criteria scored
    - Test: calls submitRating mutation on form submit (mock useMutation)
    - Test: shows delete button when user has existing rating
    - Test: pre-fills scores in edit mode
    - Use renderWithProviders, mock the query hooks with vi.mock or msw-style mocking
  </action>
  <verify>
    <automated>npx vitest run src/features/ratings/ui/RatingForm.test.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <done>Recipe detail page shows rating breakdown with per-criterion averages. Users can submit/edit/delete ratings. Favorites page at /favorites shows user's favorited recipes in a card grid. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc -b --noEmit` passes
- `npx vitest run src/features/recipes/ui/ServingScaler.test.tsx` passes
- `npx vitest run src/features/ratings/ui/RatingForm.test.tsx` passes
- `npm test` passes (all tests green)
- Recipe detail page renders all sections at /recipes/$recipeId
- Serving scaler adjusts ingredient amounts
- Rating breakdown displays per-criterion averages
- Rating form allows submit/edit/delete
- Favorites page shows favorited recipes
</verification>

<success_criteria>
- Recipe detail page displays hero image, title, metadata, ingredients, steps, ratings
- Serving count +/- adjusts ingredient amounts with correct fractional arithmetic
- Multi-criteria rating breakdown shows averages per criterion
- User can submit ratings with per-criterion star scores
- User can edit or delete their own rating
- Favorite heart works on detail page (same as cards)
- /favorites page shows user's favorited recipes with empty state when none
</success_criteria>

<output>
After completion, create `.planning/phases/02-recipe-browsing-ratings-and-profiles/02-03-SUMMARY.md`
</output>
