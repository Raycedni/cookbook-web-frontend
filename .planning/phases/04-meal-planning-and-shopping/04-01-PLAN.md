---
phase: 04-meal-planning-and-shopping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/meals/api/types.ts
  - src/features/meals/api/meal-plan-api.ts
  - src/features/meals/api/meal-plan-queries.ts
  - src/routes/_authenticated/meal-plans.tsx
  - src/routes/_authenticated/meal-plans/new.tsx
  - src/routes/_authenticated/meal-plans/$planId.tsx
  - src/routes/_authenticated/meal-plans/$planId.shopping.tsx
  - src/shared/ui/NavBar.tsx
  - src/shared/ui/MobileNav.tsx
autonomous: true
requirements:
  - MEAL-01
  - MEAL-02
  - MEAL-03
  - MEAL-04
  - MEAL-05
  - MEAL-06
  - MEAL-07
  - SHOP-01
  - SHOP-02
  - SHOP-03

must_haves:
  truths:
    - "All meal plan domain types are exported and usable by downstream plans"
    - "API functions exist for all meal plan and shopping list operations"
    - "Query factory follows established recipeQueries pattern"
    - "All four routes are registered and render placeholder content"
    - "NavBar and MobileNav link to /meal-plans (no longer disabled)"
  artifacts:
    - path: "src/features/meals/api/types.ts"
      provides: "MealPlan, MealSlotType, MealAssignment, GeneratePreferences, ShoppingList, ShoppingCategory, ShoppingItem types"
      exports: ["MealPlan", "MealSlotType", "MealAssignment", "GeneratePreferences", "ShoppingList", "ShoppingCategory", "ShoppingItem", "CreateMealPlanRequest", "UpdateMealPlanRequest", "AssignMealRequest", "GenerateRequest"]
    - path: "src/features/meals/api/meal-plan-api.ts"
      provides: "All API functions for meal plan CRUD, meal assignment, generation, slot management, shopping list"
      exports: ["getMealPlans", "getMealPlan", "createMealPlan", "updateMealPlan", "deleteMealPlan", "assignMeal", "removeMeal", "generateMeals", "addMealSlot", "updateMealSlot", "removeMealSlot", "getShoppingList", "toggleShoppingItem"]
    - path: "src/features/meals/api/meal-plan-queries.ts"
      provides: "TanStack Query factory for meal plans and shopping lists"
      exports: ["mealPlanQueries"]
    - path: "src/routes/_authenticated/meal-plans.tsx"
      provides: "Meal plans list route"
    - path: "src/routes/_authenticated/meal-plans/$planId.tsx"
      provides: "Meal plan calendar view route"
  key_links:
    - from: "src/features/meals/api/meal-plan-api.ts"
      to: "src/shared/api/client.ts"
      via: "apiClient import"
      pattern: "import.*apiClient.*from.*shared/api/client"
    - from: "src/features/meals/api/meal-plan-queries.ts"
      to: "src/features/meals/api/meal-plan-api.ts"
      via: "queryFn references"
      pattern: "queryFn.*getMealPlan"
    - from: "src/shared/ui/NavBar.tsx"
      to: "/meal-plans"
      via: "Link component"
      pattern: "to=\"/meal-plans\""
---

<objective>
Create the complete foundation layer for Phase 4: domain types, API functions, query factory, route scaffolds, and nav updates.

Purpose: All subsequent plans depend on these types, API functions, and routes. Building this first eliminates the "scavenger hunt" anti-pattern and lets Plans 02-05 focus purely on UI implementation.
Output: types.ts, meal-plan-api.ts, meal-plan-queries.ts, 4 route files, updated NavBar/MobileNav
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meal-planning-and-shopping/04-CONTEXT.md
@.planning/phases/04-meal-planning-and-shopping/04-RESEARCH.md

@src/features/recipes/api/types.ts
@src/features/recipes/api/recipe-queries.ts
@src/features/recipes/api/recipe-api.ts
@src/shared/api/client.ts
@src/shared/ui/NavBar.tsx
@src/shared/ui/MobileNav.tsx

<interfaces>
<!-- Existing types this plan must reference -->

From src/features/recipes/api/types.ts:
```typescript
export interface RecipeSummary {
  id: string
  title: string
  imageUrl: string | null
  cookTimeMinutes: number
  averageRating: number
  tags: Tag[]
  isFavorite: boolean
}
```

From src/features/recipes/api/recipe-queries.ts:
```typescript
// Pattern to follow for mealPlanQueries:
export const recipeQueries = {
  all: () => ['recipes'] as const,
  list: (filters) => infiniteQueryOptions({ queryKey: [...recipeQueries.all(), 'list', filters], ... }),
  detail: (id) => queryOptions({ queryKey: [...recipeQueries.all(), 'detail', id], ... }),
}
```

From src/shared/ui/NavBar.tsx (line 37):
```typescript
<span className="text-white/30 cursor-not-allowed">Meal Plans</span>
// ^ Replace this with active Link
```

From src/shared/ui/MobileNav.tsx (line 17):
```typescript
{ to: '/', icon: CalendarDays, label: 'Meals', disabled: true },
// ^ Change to: { to: '/meal-plans', icon: CalendarDays, label: 'Meals' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meal plan domain types, API functions, and query factory</name>
  <files>src/features/meals/api/types.ts, src/features/meals/api/meal-plan-api.ts, src/features/meals/api/meal-plan-queries.ts</files>
  <action>
Create `src/features/meals/api/` directory with three files:

**types.ts** -- All domain types per RESEARCH.md code examples section:
- `MealPlan` (id, name, startDate, endDate, participants, mealSlots, meals, generatePreferences?)
- `MealSlotType` (id, name, orderIndex)
- `MealAssignment` (id, dayIndex, mealSlotId, recipe: RecipeSummary, isGenerated)
- `GeneratePreferences` (mealSlotIds, tagIds, favoritesOnly, maxCookTimeMinutes, fillMode, maxRepeats)
- `GenerateRequest` (preferences: GeneratePreferences)
- `ShoppingList` (planId, categories, totalEstimatedCost)
- `ShoppingCategory` (name, items)
- `ShoppingItem` (id, ingredientName, aggregatedAmount, unit, recipeNames, estimatedCost, checked)
- `CreateMealPlanRequest` (name, startDate, endDate, participants)
- `UpdateMealPlanRequest` (all optional: name?, startDate?, endDate?, participants?)
- `AssignMealRequest` (dayIndex, mealSlotId, recipeId)
- Import `RecipeSummary` from `@/features/recipes/api/types`

**meal-plan-api.ts** -- All API functions using ky-based apiClient per RESEARCH.md:
- `getMealPlans()` -> `MealPlan[]`
- `getMealPlan(id)` -> `MealPlan`
- `createMealPlan(data)` -> `MealPlan`
- `updateMealPlan(id, data)` -> `MealPlan`
- `deleteMealPlan(id)` -> `void`
- `assignMeal(planId, data)` -> `MealAssignment`
- `removeMeal(planId, mealId)` -> `void`
- `generateMeals(planId, data)` -> `MealAssignment[]`
- `addMealSlot(planId, name)` -> `MealSlotType`
- `updateMealSlot(planId, slotId, name)` -> `MealSlotType`
- `removeMealSlot(planId, slotId)` -> `void`
- `getShoppingList(planId)` -> `ShoppingList`
- `toggleShoppingItem(planId, itemId, checked)` -> `void`
- All use `apiClient` from `@/shared/api/client`
- Endpoint base path: `meal-plans` (adjust if backend differs)

**meal-plan-queries.ts** -- TanStack Query factory following recipeQueries pattern:
- `mealPlanQueries.all()` -> `['meal-plans']`
- `mealPlanQueries.list()` -> queryOptions calling getMealPlans
- `mealPlanQueries.detail(planId)` -> queryOptions calling getMealPlan
- `mealPlanQueries.shoppingList(planId)` -> queryOptions calling getShoppingList
- Import from `@tanstack/react-query` (queryOptions)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All types exported, API functions compile against apiClient, query factory exports mealPlanQueries with list/detail/shoppingList factories</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold route files and enable nav links</name>
  <files>src/routes/_authenticated/meal-plans.tsx, src/routes/_authenticated/meal-plans/new.tsx, src/routes/_authenticated/meal-plans/$planId.tsx, src/routes/_authenticated/meal-plans/$planId.shopping.tsx, src/shared/ui/NavBar.tsx, src/shared/ui/MobileNav.tsx</files>
  <action>
Create 4 route files following TanStack Router file-based routing conventions (see existing routes like recipes.tsx for pattern):

**meal-plans.tsx** (`/meal-plans`):
- `createFileRoute('/_authenticated/meal-plans')` with component that renders a placeholder GlassPanel with title "Meal Plans" and EmptyState. This will be replaced by MealPlanListPage in Plan 02.

**meal-plans/new.tsx** (`/meal-plans/new`):
- `createFileRoute('/_authenticated/meal-plans/new')` with placeholder "Create Meal Plan" heading in GlassPanel.

**meal-plans/$planId.tsx** (`/meal-plans/$planId`):
- `createFileRoute('/_authenticated/meal-plans/$planId')` with placeholder reading planId from `Route.useParams()`. Shows "Meal Plan: {planId}" in GlassPanel.

**meal-plans/$planId.shopping.tsx** (`/meal-plans/$planId/shopping`):
- `createFileRoute('/_authenticated/meal-plans/$planId/shopping')` with placeholder "Shopping List for {planId}" in GlassPanel.

Create the `meal-plans/` directory under `_authenticated/` for nested routes.

**NavBar.tsx** -- Replace the disabled "Meal Plans" span (line 37) with an active Link:
```tsx
<Link
  to="/meal-plans"
  className="text-white/70 hover:text-white transition-colors [&.active]:text-accent"
>
  Meal Plans
</Link>
```
Import CalendarDays icon is NOT needed in NavBar (it uses text only). Just change span to Link.

**MobileNav.tsx** -- Update the Meals nav item (line 17):
- Change `to: '/'` to `to: '/meal-plans'`
- Remove `disabled: true`
- Result: `{ to: '/meal-plans', icon: CalendarDays, label: 'Meals' }`

After creating route files, run `npx tanstack-router generate` (or the dev server will auto-generate) to update the route tree.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>All 4 routes render placeholder content, NavBar shows active "Meal Plans" link, MobileNav "Meals" item navigates to /meal-plans</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run` existing tests still pass
- Types file exports all 11+ types/interfaces
- API file exports all 13 functions
- Query factory exports mealPlanQueries with 4 query factories
- Route files exist and follow TanStack Router createFileRoute pattern
- NavBar and MobileNav link to /meal-plans (not disabled)
</verification>

<success_criteria>
Foundation layer complete: all downstream plans can import types, call API functions, use query factories, and navigate to route pages without creating any of these from scratch.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planning-and-shopping/04-01-SUMMARY.md`
</output>
