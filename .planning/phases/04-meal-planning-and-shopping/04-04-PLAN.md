---
phase: 04-meal-planning-and-shopping
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/features/meals/ui/RecipeDragPanel.tsx
  - src/features/meals/ui/DraggableRecipe.tsx
  - src/features/meals/ui/AutoFillModal.tsx
  - src/features/meals/ui/WeeklyCalendar.tsx
  - src/features/meals/ui/CalendarCell.tsx
  - src/routes/_authenticated/meal-plans/$planId.tsx
autonomous: true
requirements:
  - MEAL-03
  - MEAL-05

must_haves:
  truths:
    - "User can drag a recipe from the side panel onto a calendar slot to assign it"
    - "User can open auto-fill settings modal with 4 preference parameters plus fill mode and repeat tolerance"
    - "User can auto-generate meals and see skeleton shimmer in affected slots during generation"
    - "User can regenerate all meals or refresh a single slot"
    - "Generated meals show a 'Generated' badge state"
    - "Preferences persist per plan (pre-filled on next auto-fill)"
  artifacts:
    - path: "src/features/meals/ui/RecipeDragPanel.tsx"
      provides: "Collapsible side panel with searchable draggable recipe list"
      min_lines: 40
    - path: "src/features/meals/ui/DraggableRecipe.tsx"
      provides: "useDraggable wrapper for recipe items in drag panel"
      min_lines: 15
    - path: "src/features/meals/ui/AutoFillModal.tsx"
      provides: "Auto-generate settings modal with all preference parameters"
      min_lines: 80
  key_links:
    - from: "src/features/meals/ui/WeeklyCalendar.tsx"
      to: "@dnd-kit/core"
      via: "DndContext wrapping calendar grid + drag panel"
      pattern: "DndContext"
    - from: "src/features/meals/ui/DraggableRecipe.tsx"
      to: "@dnd-kit/core"
      via: "useDraggable hook"
      pattern: "useDraggable"
    - from: "src/features/meals/ui/CalendarCell.tsx"
      to: "@dnd-kit/core"
      via: "useDroppable hook"
      pattern: "useDroppable"
    - from: "src/features/meals/ui/AutoFillModal.tsx"
      to: "src/features/meals/api/meal-plan-api.ts"
      via: "generateMeals mutation"
      pattern: "generateMeals"
---

<objective>
Add drag-and-drop recipe assignment (power-user flow) and auto-generate meal plan functionality with preference settings.

Purpose: Drag-and-drop provides a fast power-user workflow for meal assignment. Auto-generate lets users quickly fill their plan based on dietary preferences.
Output: RecipeDragPanel.tsx, DraggableRecipe.tsx, AutoFillModal.tsx, updated WeeklyCalendar/CalendarCell/$planId
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meal-planning-and-shopping/04-CONTEXT.md
@.planning/phases/04-meal-planning-and-shopping/04-RESEARCH.md
@.planning/phases/04-meal-planning-and-shopping/04-01-SUMMARY.md
@.planning/phases/04-meal-planning-and-shopping/04-03-SUMMARY.md

@src/features/meals/api/types.ts
@src/features/meals/api/meal-plan-api.ts
@src/features/meals/api/meal-plan-queries.ts
@src/features/meals/ui/WeeklyCalendar.tsx
@src/features/meals/ui/CalendarCell.tsx
@src/features/recipes/api/types.ts
@src/features/recipes/api/recipe-queries.ts
@src/features/recipes/ui/SearchBar.tsx
@src/shared/ui/useDelayedLoading.ts

<interfaces>
From src/features/meals/api/types.ts:
```typescript
export interface GeneratePreferences {
  mealSlotIds: string[]
  tagIds: number[]
  favoritesOnly: boolean
  maxCookTimeMinutes: number | null
  fillMode: 'empty_only' | 'replace_all'
  maxRepeats: number
}
export interface GenerateRequest { preferences: GeneratePreferences }
export interface MealAssignment {
  id: string; dayIndex: number; mealSlotId: string;
  recipe: RecipeSummary; isGenerated: boolean;
}
```

From src/features/meals/api/meal-plan-api.ts:
```typescript
export async function generateMeals(planId: string, data: GenerateRequest): Promise<MealAssignment[]>
export async function assignMeal(planId: string, data: AssignMealRequest): Promise<MealAssignment>
```

From @dnd-kit/core (installed):
```typescript
import { DndContext, DragOverlay, DragEndEvent, useDraggable, useDroppable } from '@dnd-kit/core'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drag-and-drop recipe assignment with DndContext, DragOverlay, and collapsible panel</name>
  <files>src/features/meals/ui/RecipeDragPanel.tsx, src/features/meals/ui/DraggableRecipe.tsx, src/features/meals/ui/WeeklyCalendar.tsx, src/features/meals/ui/CalendarCell.tsx, src/routes/_authenticated/meal-plans/$planId.tsx</files>
  <action>
**DraggableRecipe.tsx:**
- Props: `recipe: RecipeSummary`
- Use `useDraggable({ id: \`recipe-\${recipe.id}\`, data: { type: 'recipe', recipe } })`
- Render compact recipe card: small image, title, cook time, rating
- Apply `opacity-30` when `isDragging`, `cursor-grab` normally
- Spread `...listeners` and `...attributes` on the root div, `ref={setNodeRef}`

**RecipeDragPanel.tsx:**
- Collapsible side panel (right side of calendar on desktop, hidden on mobile)
- Toggle button in the $planId toolbar: "Recipe Panel" with ChevronRight/ChevronLeft icon
- Panel content: search input (with useDebounce), scrollable list of DraggableRecipe items
- Use `useInfiniteQuery(recipeQueries.list({ q: debouncedSearch }))` for recipe data
- Load more button at bottom if hasNextPage
- Panel width: ~250px, GlassPanel styling, max-h matching calendar height with overflow-y-auto
- Hide on mobile (drag flow is power-user only; mobile uses modal flow per CONTEXT.md)

**WeeklyCalendar.tsx updates:**
- Wrap the calendar grid (desktop layout) AND the RecipeDragPanel in a single `DndContext`
- IMPORTANT: Keep this DndContext separate from any other DndContext (per Phase 3 decision). Do NOT nest DndContexts.
- Add `onDragEnd` handler: extract recipe from `active.data.current.recipe`, extract `dayIndex` and `slotId` from `over.data.current`, call `assignMeal` mutation
- Add `DragOverlay` at root of DndContext: renders a compact recipe card preview while dragging (z-50)
- Track `activeDragItem` in state via `onDragStart`/`onDragEnd` to control DragOverlay content
- Use PointerSensor with `activationConstraint: { distance: 8 }` to distinguish click from drag

**CalendarCell.tsx updates:**
- Add `useDroppable({ id: \`cell-\${dayIndex}-\${slotId}\`, data: { dayIndex, slotId } })`
- Apply `ref={setNodeRef}` to the cell div
- When `isOver` is true: show accent border highlight (`border-accent bg-accent/10`)
- This makes each cell a valid drop target

**$planId.tsx updates:**
- Add RecipeDragPanel rendering next to WeeklyCalendar in a flex layout
- Toggle state for panel visibility (default collapsed on desktop, hidden on mobile)
- Layout: `flex gap-4` with calendar taking remaining space and panel at fixed width
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Dragging a recipe from the side panel onto a calendar cell assigns it to that slot via API mutation, DragOverlay shows recipe preview during drag, cells highlight on hover</done>
</task>

<task type="auto">
  <name>Task 2: Build AutoFillModal with all preference parameters and generation flow</name>
  <files>src/features/meals/ui/AutoFillModal.tsx, src/routes/_authenticated/meal-plans/$planId.tsx</files>
  <action>
**AutoFillModal.tsx:**
- Props: `isOpen: boolean`, `onClose: () => void`, `planId: string`, `plan: MealPlan`
- Modal overlay (same pattern as RecipePickerModal): fixed inset-0 bg-black/60 z-50, centered GlassPanel
- Pre-fill form state from `plan.generatePreferences` if it exists (preferences persist per plan)
- Form fields (all managed with useState):
  1. **Meal types to fill**: checkbox list of plan's meal slot types (`plan.mealSlots`). Each checkbox toggles a slotId in `mealSlotIds` array. Default: all selected.
  2. **Tag/category preferences**: reuse TagTree/TagChips components or a simplified multi-select of tags. Use `useQuery(recipeQueries.tags())` to load tags. Selected tagIds stored in state.
  3. **Use only favorites**: toggle switch (styled checkbox or button), maps to `favoritesOnly: boolean`
  4. **Max cook time**: range slider or number input with "minutes" label. Null = no limit (show "No limit" label when null). Include a checkbox "Set max cook time" to toggle between null and a value.
  5. **Fill mode**: radio buttons -- "Fill empty slots only" vs "Replace all"
  6. **Repeat tolerance**: number input -- "Max times a recipe can appear" (default: 2, min: 1)
- "Generate" button: calls `useMutation` with `generateMeals(planId, { preferences })`, invalidates `mealPlanQueries.detail(planId)` on success, closes modal
- Loading state during generation: show spinner on the Generate button, disable form

**$planId.tsx updates:**
- Wire the "Auto-fill" toolbar button to open AutoFillModal
- Add "Regenerate" button in toolbar: re-opens AutoFillModal with existing preferences pre-filled
- Add per-slot refresh icon on generated meals (CalendarCell needs a `onRegenerate?: () => void` prop):
  - When clicked, call `generateMeals` with preferences but only for that specific slot (set mealSlotIds to just that slot, fillMode to 'replace_all')
  - Show skeleton shimmer in that cell during regeneration using useDelayedLoading
- While auto-generate mutation is pending: show Skeleton shimmer in all affected calendar cells
  - Use the mutation's `isPending` state combined with the mealSlotIds from preferences to determine which cells show skeletons
  - Apply useDelayedLoading (200ms delay + 500ms minimum) for consistent loading experience
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Auto-fill modal shows all 6 preference controls, generation shows skeleton loading in affected slots, preferences persist per plan, per-slot regenerate works, Regenerate button re-uses existing preferences</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` existing tests pass
- Drag recipe from panel to calendar cell assigns it
- DragOverlay shows recipe preview during drag
- Calendar cells highlight when dragged over
- Auto-fill modal has all 4 preference params + fill mode + repeat tolerance
- Generation shows skeleton shimmer in affected cells
- Per-slot refresh icon regenerates a single slot
- Preferences pre-fill from plan.generatePreferences
</verification>

<success_criteria>
Power users can drag recipes from the side panel to calendar slots. Users can auto-generate meal plans with full preference control, see loading feedback during generation, and fine-tune individual slots with per-slot regeneration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planning-and-shopping/04-04-SUMMARY.md`
</output>
