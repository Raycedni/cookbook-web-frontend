---
phase: 04-meal-planning-and-shopping
plan: 05
type: execute
wave: 3
depends_on: ["04-01"]
files_modified:
  - src/features/meals/ui/ShoppingListPage.tsx
  - src/features/meals/ui/ShoppingCategory.tsx
  - src/features/meals/ui/ShoppingItem.tsx
  - src/routes/_authenticated/meal-plans/$planId.shopping.tsx
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03

must_haves:
  truths:
    - "User can navigate to a shopping list page from a meal plan"
    - "Shopping list shows ingredients grouped by category in collapsible sections"
    - "Each ingredient row shows aggregated amount, unit, and which recipes need it"
    - "Each ingredient row shows per-item estimated cost"
    - "Total estimated cost is displayed at the bottom of the list"
    - "User can check off items with strikethrough + dimmed styling that stay in place"
  artifacts:
    - path: "src/features/meals/ui/ShoppingListPage.tsx"
      provides: "Full shopping list page with categories, total cost, check-off state"
      min_lines: 50
    - path: "src/features/meals/ui/ShoppingCategory.tsx"
      provides: "Collapsible category section with items and checked count"
      min_lines: 30
    - path: "src/features/meals/ui/ShoppingItem.tsx"
      provides: "Single ingredient row with checkbox, amount, unit, recipe sources, cost"
      min_lines: 20
  key_links:
    - from: "src/features/meals/ui/ShoppingListPage.tsx"
      to: "src/features/meals/api/meal-plan-queries.ts"
      via: "useQuery with mealPlanQueries.shoppingList(planId)"
      pattern: "mealPlanQueries\\.shoppingList"
    - from: "src/features/meals/ui/ShoppingItem.tsx"
      to: "src/features/meals/api/meal-plan-api.ts"
      via: "toggleShoppingItem mutation or localStorage fallback"
      pattern: "(toggleShoppingItem|localStorage)"
    - from: "src/routes/_authenticated/meal-plans/$planId.shopping.tsx"
      to: "src/features/meals/ui/ShoppingListPage.tsx"
      via: "component import"
      pattern: "import.*ShoppingListPage"
---

<objective>
Build the shopping list page with ingredient aggregation display, collapsible category sections, cost estimation, and check-off functionality.

Purpose: Shopping lists are the primary output of meal planning -- users need a clear, grouped list of what to buy, with cost awareness and progress tracking.
Output: ShoppingListPage.tsx, ShoppingCategory.tsx, ShoppingItem.tsx, updated shopping route
</objective>

<execution_context>
@/home/lukas/.claude/get-shit-done/workflows/execute-plan.md
@/home/lukas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meal-planning-and-shopping/04-CONTEXT.md
@.planning/phases/04-meal-planning-and-shopping/04-RESEARCH.md
@.planning/phases/04-meal-planning-and-shopping/04-01-SUMMARY.md

@src/features/meals/api/types.ts
@src/features/meals/api/meal-plan-api.ts
@src/features/meals/api/meal-plan-queries.ts
@src/shared/ui/GlassPanel.tsx
@src/shared/ui/Skeleton.tsx
@src/shared/ui/EmptyState.tsx
@src/shared/ui/useDelayedLoading.ts

<interfaces>
From src/features/meals/api/types.ts:
```typescript
export interface ShoppingList {
  planId: string
  categories: ShoppingCategory[]
  totalEstimatedCost: number
}

export interface ShoppingCategory {
  name: string              // "Dairy", "Produce", "Meat", "Pantry", etc.
  items: ShoppingItem[]
}

export interface ShoppingItem {
  id: string
  ingredientName: string
  aggregatedAmount: number
  unit: string
  recipeNames: string[]     // which recipes need this ingredient
  estimatedCost: number
  checked: boolean
}
```

From src/features/meals/api/meal-plan-api.ts:
```typescript
export async function getShoppingList(planId: string): Promise<ShoppingList>
export async function toggleShoppingItem(planId: string, itemId: string, checked: boolean): Promise<void>
```

From src/features/meals/api/meal-plan-queries.ts:
```typescript
export const mealPlanQueries = {
  shoppingList: (planId: string) => queryOptions({ ... }),
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ShoppingCategory and ShoppingItem components</name>
  <files>src/features/meals/ui/ShoppingCategory.tsx, src/features/meals/ui/ShoppingItem.tsx</files>
  <action>
**ShoppingItem.tsx:**
- Props: `item: ShoppingItem`, `onToggle: (itemId: string, checked: boolean) => void`
- Layout: flex row with checkbox, ingredient info, and cost
  - Checkbox: native input[type="checkbox"] styled with accent color, checked state from item.checked
  - Ingredient name: text, applies `line-through opacity-50` when checked
  - Amount + unit: e.g., "500 g" or "2 cups" -- stays in place when checked (no reflow per CONTEXT.md)
  - Recipe sources: small sub-line in text-white/40, e.g., "Pasta, Curry" (from item.recipeNames joined with comma)
  - Estimated cost: right-aligned, formatted with `Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' })` (EUR default, adjust if backend provides currency)
- On checkbox change: call `onToggle(item.id, !item.checked)`
- Transition: smooth opacity transition when checking/unchecking

**ShoppingCategory.tsx:**
- Props: `category: ShoppingCategoryType` (rename import to avoid clash), `onToggleItem: (itemId: string, checked: boolean) => void`
- Collapsible section using local `useState(true)` for expanded state
- Header: click to toggle expanded, shows:
  - Category name (e.g., "Dairy") in white font-medium
  - Checked count: "{checked}/{total}" in text-white/40
  - ChevronDown icon that rotates 180deg when expanded (transition-transform)
- Body (when expanded): list of ShoppingItem components in `space-y-1`
- Wrap in GlassPanel with overflow-hidden
- Category subtotal: small text at bottom showing sum of item costs in the category
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>ShoppingItem renders checkbox with strikethrough on check, recipe source sub-line, and cost. ShoppingCategory renders collapsible section with items and checked count.</done>
</task>

<task type="auto">
  <name>Task 2: Build ShoppingListPage and wire into route</name>
  <files>src/features/meals/ui/ShoppingListPage.tsx, src/routes/_authenticated/meal-plans/$planId.shopping.tsx</files>
  <action>
**ShoppingListPage.tsx:**
- Props: `planId: string`
- Fetch shopping list: `useQuery(mealPlanQueries.shoppingList(planId))`
- Loading state: Skeleton rows in GlassPanel sections (use useDelayedLoading)
- Empty state: EmptyState component ("No items in shopping list. Add recipes to your meal plan first.")
- Main content: list of ShoppingCategory components for each category
- Check-off persistence strategy (Claude's discretion per CONTEXT.md):
  - Primary: try backend via `toggleShoppingItem` mutation
  - Fallback: if mutation fails (e.g., 404/501), persist to `localStorage` key `shopping-checked-${planId}` as `{ [itemId]: boolean }`
  - On load: merge localStorage checked state with server state (localStorage takes precedence for checked items)
  - Implementation: create a `useShoppingCheckOff(planId)` hook that encapsulates this logic
  - Mutation: optimistic update via `queryClient.setQueryData` in `onMutate` to toggle checked state instantly
- Total cost display: sticky footer or bottom section in GlassPanel showing total formatted as currency
  - Show total and "checked off" subtotal (sum of checked items vs total)
  - Format: "Total: EUR XX.XX" and "Remaining: EUR YY.YY"
- Back navigation: "Back to Plan" link at top, navigates to `/meal-plans/${planId}`
- Page title: "Shopping List" with plan name subtitle

**$planId.shopping.tsx route:**
- Replace placeholder with ShoppingListPage, passing planId from `Route.useParams()`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Shopping list page fetches and displays grouped ingredients, check-off works with optimistic updates and localStorage fallback, total/remaining costs displayed</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` existing tests pass
- Shopping list fetches data via mealPlanQueries.shoppingList
- Categories render as collapsible sections
- Each item shows aggregated amount, unit, recipe sources, cost
- Check-off applies strikethrough + dimmed styling
- Checked items stay in place (no reflow)
- Total and remaining costs displayed
- Back navigation to meal plan calendar works
</verification>

<success_criteria>
Users can view a complete shopping list grouped by category with ingredient amounts, recipe sources, and costs. They can check off items as they shop with visual feedback and persistence. Total cost and remaining cost are always visible.
</success_criteria>

<output>
After completion, create `.planning/phases/04-meal-planning-and-shopping/04-05-SUMMARY.md`
</output>
